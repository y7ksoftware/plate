{#
 # Frontend Constants
 # ---------------
 #
 # Sends config and .env configs from the backend to JS
 #
 # 1. Define the desired config in one of the craft config files (see config/ folder). You can load a ENV config setting there
 # 2. Add the config to the object below like
 # 'CONFIG_NAME': craft.app.config.general.myConfig
 # 'CONFIG_NAME': craft.app.config.myConfigFile.myConfig
 #
 #}

<script type="text/javascript">

    window.App = {};
    window.App.constants = {{ {
        'APP_DEBUG': craft.app.config.general.devMode,
        'BUGSNAG_API_KEY_JS': craft.app.config.general.bugsnagFrontendApiKey,
    }|json_encode|raw }};

</script>
